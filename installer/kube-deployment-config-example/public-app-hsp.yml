apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: public-app
  namespace: divoc
spec:
  maxReplicas: 5
  minReplicas: 1
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: public-app
  targetCPUUtilizationPercentage: 70
